display_name: AutoResearch Workflow
short_description: Drives $autoreskill and /goal state, stage gates, job packets, and recovery.
default_prompt: Use $autoreskill or /goal to initialize, resume, advance, or debug a portable AutoResearch workflow with bounded autopilot.
